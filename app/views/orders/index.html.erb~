<h1><%= t("orders.listing_orders") %></h1>

<table>
  <thead>
    <tr>
      <th><%= t("orders.order_status") %></th>
      <th><%= t("orders.payment_method") %></th>
      <th><%= t("orders.shipping_address") %></th>
      <th><%= t("orders.shipping_city") %></th>
      <th><%= t("orders.customer_name") %></th>
      <th><%= t("orders.customer_lastname") %></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.order_status %></td>
        <td><%= order.payment_method %></td>
        <td><%= order.shipping_address %></td>
        <td><%= order.shipping_city %></td>
        <td><%= order.customer_name %></td>
        <td><%= order.customer_lastname %></td>
        <td><%= link_to t("interface.show"), order_path(id: order.id) %></td>
        <td><%= link_to t("interface.edit"), edit_order_path(id: order.id) %></td>
        <td><%= link_to t("interface.destroy"), order_path(id: order.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t("orders.new_order"), new_order_path %>
